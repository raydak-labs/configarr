# Plan: live arr-e2e (#526)

Spec: [`.ai/docs/specs/2026-09-16-arr-e2e-design.md`](../specs/2026-09-16-arr-e2e-design.md)

## Steps (as executed)

1. **Product fixes the suite uncovered.** Persist disabled download clients and indexers with `forceSave` (`src/clients/connection.ts` + the six clients). Move Prowlarr's unmanaged deletes behind their dependents (tags after download clients, sync profiles and proxies after indexers), report download-client failures so tag cleanup can be skipped, and filter deferred deletes against the validated config set.
2. **Stack.** `tests/arr-e2e/docker-compose.yml`: five media *arr + Prowlarr + FlareSolverr, `/data/e2e[-b]` binds, auth via `<APP>__AUTH__*` (Whisparr seeds a fixture `config.xml` from its entrypoint).
3. **Suite.** One file per *arr with literal payloads (`sonarr` … `lidarr`, `prowlarr`) plus `pipeline.e2e.test.ts` for the multi-instance run. `helpers.ts` holds the clients, configarr runner, assertions, cleanup and baseline snapshot/restore; `config.ts` only the fragments identical for every *arr. `globalSetup.ts` waits for the APIs and warms the template repo clone. Per-*arr files run in parallel, the pipeline file alone.
4. **CI.** `Arr E2E` workflow (`workflow_dispatch`) waits for all seven endpoints and runs `pnpm test:e2e:arr` with the containers as the runner user.
5. **Docs.** `tests/arr-e2e/README.md` is the coverage source of truth (run instructions, ports, matrix, gaps, rules for new features); pointer in AGENTS.md / CLAUDE.md; this plan and its spec.
6. **Image maintenance.** Whisparr pinned to `v3-3.5.0-release.1585`; Renovate gets regex versioning for hotio and LinuxServer develop tags, and `tests/**` is no longer in `ignorePaths`.
7. **Verify.** `pnpm build && pnpm test && pnpm lint && pnpm typecheck`, plus `pnpm test:e2e:arr` against the live stack (45 tests parallel + 1 pipeline).
